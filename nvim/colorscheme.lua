require('sand').colorscheme()
-- local colors = require('ayu.colors')
-- colors.generate() -- Pass `true` to enable mirage

-- require('ayu').setup({
--     mirage = false,
--     overrides = {
--         ErrorMsg = { fg = "#FFFFFF" }
--     }
-- })
